import React from "react";

const PokemonImage = (props) => {
    console.log("image",props)
    return (
        <div>
            {/* {props.image && <img key={props.image.image.name} src={props.image.image.url} />} */}
        </div>
    )
}

export default PokemonImage;